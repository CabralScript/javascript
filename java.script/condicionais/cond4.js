//operador ternário

// use "?" como se fosse if && ":" como se fosse else

const prompt = require('readline-sync')

const idade = prompt.question("Qual eh a sua idade?")

const result = idade >= 18 ? "Você é de maior" : "Você é de menor"

console.log(result)






